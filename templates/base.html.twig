<!doctype html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="Application de gestion de tickets">
    <meta name="author" content="Mark Otto, Jacob Thornton, and Bootstrap contributors">
    <meta name="generator" content="Jekyll v4.1.1">
    <title>{% block title %}Tickets manager{% endblock %}</title>

    <!-- Bootstrap core CSS -->
    <link href="{{ asset('assets/css/bootstrap.min.css') }}" rel="stylesheet">
    <link href="{{ asset('assets/css/attribution.css') }}" rel="stylesheet">
    <link rel="shortcut icon" href="#">
    {# use vue js online (cdn) #}
    {# <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script> #}
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <style>
        .bd-placeholder-img {
            font-size: 1.125rem;
            text-anchor: middle;
            -webkit-user-select: none;
            -moz-user-select: none;
            -ms-user-select: none;
            user-select: none;
        }

        @media (min-width: 768px) {
            .bd-placeholder-img-lg {
                font-size: 3.5rem;
            }
        }
    </style>
    <!-- Custom styles for this template -->
    <link href="{{ asset("assets/css/carousel.css") }}" rel="stylesheet">
</head>
<body>
{% block javascript %}
<script type="text/javascript">
    $(document).ready(function(){

});
</script>
{% endblock %}
<header>
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
        {% if app.user %}
        <div class="navbar-item navbar-collapse justify-content-end ">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item active">
                    <a class="nav-link" href="{{ path('demande') }}">Demandes</a>
                </li>
            </ul>
        </div>
        {% endif %}
        <div class="navbar-item navbar-collapse justify-content-end ">
            {% if app.user %}
                <a style="color: white" href="{{ path('demande') }}"></a> | <a
                    href="{{ path("app_logout") }}">Se déconnecter</a>
            {% else %}
                <a href="{{ path("app_login") }}">Connexion</a> |  <a
                    href="{{ path("inscription") }}">Inscription</a>
            {% endif %}
        </div>

    </nav>
</header>

<main role="main">
    <div class="container marketing mt-5 %}">

        {% block content %}
        {% endblock %}

    </div><!-- /.container -->

    <!-- FOOTER -->
    <footer class="footer-custom">
    </footer>
</main>
<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
<script src="{{ asset('assets/js/bootstrap.bundle.js') }}"></script>
</body>
</html>